# Frontier Lang Rust Adapter Notes

Keep this package as a projection adapter. Runtime-neutral semantic graph changes belong in `@shapeshift-labs/frontier-lang-kernel`.

Do not print or commit npm tokens. Release credentials live in `/Users/james/Documents/json-diff/.env`.
